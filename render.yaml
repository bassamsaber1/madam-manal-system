services:
  - type: web
    name: data-egypt
    runtime: node
    plan: free
    buildCommand: git lfs pull && npm install && node scripts/ensure-index.js && npm run build
    startCommand: npm start
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: "20.18.0"
      - key: NODE_ENV
        value: production
      - key: SEARCH_DB_URL
        sync: false
